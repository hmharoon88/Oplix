rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write for everyone (authenticated or not)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
